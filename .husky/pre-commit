if [[ $(git rev-parse --abbrev-ref HEAD) == 'main' ]];
 then echo && echo && echo DANGER! Preventing push to main! && echo && echo && exit 1;
 else npm run tests:unit:quiet && npm run tests:integration:quiet && npx lint-staged;
fi

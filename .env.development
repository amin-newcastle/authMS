NODE_ENV=development
PORT=5000
# DB_URI and JWT_SECRET are omitted for security. Configure via environment variables or a secure vault.
# DB_URI=mongodb+srv://<username>:<password>@<cluster>/?retryWrites=true&w=majority&appName=dev-cluster
# JWT_SECRET=<your_jwt_secret>
